# Node / React
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.vite
frontend/.eslintcache

# Java / Spring
backend/target/
backend/.idea/
backend/.classpath
backend/.project
backend/.settings/
backend/.mvn/
backend/mvnw*
backend/src/main/resources/application.properties

# Sistema
*.log
.DS_Store

*.zip

# -----------------------------------------------------------------------------
# Confidential / secrets (added to avoid committing credentials or private keys)
# -----------------------------------------------------------------------------
# Environment files
.env
*.env
*.env.local
.env.*.local
.envrc

# Credentials and service accounts
credentials.json
google-services.json
service-account.json
secret*.json
secrets.json

# Cloud provider credentials
.aws/
.aws/credentials
.azure/
.gcloud/
google-cloud-key.json

# Private keys, certificates, keystores and tokens
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.crt
*.csr
*.der
*.asc
*.token

# SSH keys (if accidentally present in repo)
id_rsa
id_rsa.pub
.ssh/

# IDE/editor settings (may contain tokens)
.vscode/
*.code-workspace

# Additional common build/log files
build/
dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# OS files
Thumbs.db
ehthumbs.db
