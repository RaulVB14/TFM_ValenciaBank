HELP.md
target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/
.templates/application.properties

### Secrets / archivos sensibles (añadidos) ###
# Archivos de entorno
.env
.env.*
!.env.example

# Propiedades y credenciales (Spring Boot / Java)
# Ignora application.properties locales que suelen contener contraseñas/URLs de BD
src/main/resources/application.properties
**/application.properties
secrets.properties
secrets.yml
credentials.yml
credentials.yml.enc

# Maven/Java local settings
.m2/
settings.xml
**/settings.xml

# Keystores, certificados y claves privadas
*.jks
*.keystore
*.p12
*.pfx
*.pem
*.key
*.crt
*.jceks

# JWT / tokens
jwt.*
jwt.secret
token*

# SSH / GPG keys
id_rsa
id_rsa.pub
id_dsa
id_dsa.pub
*.pub

# Cloud provider credentials and service accounts
aws/credentials
.aws/
google-services.json
service-account*.json

# Docker / Compose overrides with secrets
.env.local
docker-compose.override.yml

# Local IDE/workspace settings (sensitive or user-specific)
.idea/
*.iml
.vscode/

# Misc local files that may contain secrets
local.properties
*.keytab

# Backup or exported DB dumps
*.sql
*.dump

# Ignore credential files generated by CI/CD systems
# (adjust if you intentionally commit some CI templates)
secrets/**/*

# Notes:
# - Si ya has subido secretos al repo, deberás eliminarlos del índice y limpiar el historial
#   (por ejemplo: `git rm --cached <file>` y usar BFG o git filter-repo para reescribir el historial).

