# =============================================================================
# EXAMPLE FRONTEND ENVIRONMENT VARIABLES - COPY TO .env.local AND CONFIGURE
# =============================================================================
# IMPORTANTE: Renombra este archivo a '.env.local' y configura con tus valores
# NUNCA commitees archivos .env o .env.local sin .example
# =============================================================================

# Backend API Configuration
VITE_API_URL=http://localhost:8080

# Application Environment
VITE_ENV=development

# Debug Mode (true for development, false for production)
VITE_DEBUG=true

# Optional: If your API requires authentication tokens
# VITE_API_TOKEN=your_token_here
